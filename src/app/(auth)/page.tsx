import { LoginForm } from '@/features/auth/components/LoginForm';

/**
 * Root page (/) - Serves as the landing page and login entry point.
 */
export default function LandingPage() {
  return <LoginForm />;
}
